(function(){
'use strict';
const $=s=>document.querySelector(s);
function boot(){
 if(!window.THREE){setTimeout(boot,150);return}
 const host=$('.office'); if(!host){setTimeout(boot,200);return}
 if(host.dataset.cinematic==='1')return; host.dataset.cinematic='1';
 // Replace only the office visualization; preserve the rest of Nawaf HQ.
 host.innerHTML=''; host.style.position='relative'; host.style.overflow='hidden'; host.style.background='#050912';
 const THREE=window.THREE, scene=new THREE.Scene(); scene.background=new THREE.Color(0x050912); scene.fog=new THREE.FogExp2(0x050912,.018);
 const camera=new THREE.PerspectiveCamera(34,host.clientWidth/host.clientHeight,.1,120); camera.position.set(18,20,25); camera.lookAt(0,0,0);
 const renderer=new THREE.WebGLRenderer({antialias:true,alpha:false}); renderer.setPixelRatio(Math.min(devicePixelRatio,1.8)); renderer.setSize(host.clientWidth,host.clientHeight); renderer.shadowMap.enabled=true; renderer.shadowMap.type=THREE.PCFSoftShadowMap; renderer.outputColorSpace=THREE.SRGBColorSpace; renderer.toneMapping=THREE.ACESFilmicToneMapping; renderer.toneMappingExposure=1.08; host.appendChild(renderer.domElement);
 const M=(c,metal=.12,rough=.6)=>new THREE.MeshStandardMaterial({color:c,metalness:metal,roughness:rough});
 const box=(p,w,h,d,c,x,y,z,metal=.08,rough=.62)=>{const o=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),M(c,metal,rough));o.position.set(x,y,z);o.castShadow=o.receiveShadow=true;p.add(o);return o};
 const glow=(p,w,h,c,x,y,z,ry=0,op=.8)=>{const o=new THREE.Mesh(new THREE.PlaneGeometry(w,h),new THREE.MeshBasicMaterial({color:c,transparent:true,opacity:op,side:THREE.DoubleSide}));o.position.set(x,y,z);o.rotation.y=ry;p.add(o);return o};
 const floor=new THREE.Mesh(new THREE.CylinderGeometry(14.2,14.7,.45,8),M(0x111821,.35,.48));floor.position.y=-.2;floor.rotation.y=Math.PI/8;floor.receiveShadow=true;scene.add(floor);
 // Exterior terraces and illuminated paths.
 for(let i=0;i<8;i++){const a=i*Math.PI/4;const path=box(scene,2.25,.08,5.0,0x172333,Math.sin(a)*6.3,.08,Math.cos(a)*6.3,.2,.48);path.rotation.y=a;const strip=box(scene,1.55,.025,4.2,i%2?0xd5a44f:0x3d75d9,Math.sin(a)*6.3,.135,Math.cos(a)*6.3,.15,.3);strip.rotation.y=a;}
 const themes=[0xd6a64f,0x4b91ff,0xb68cff,0x63c6ff,0xe3a45f,0x5aa5d8,0xc39a6b,0x6d88ff];
 const names=['الإدارة','التقنية','المنتج','الجودة','التحليلات','الأنظمة','البحث','العمليات'];
 const bots=[];
 function robot(parent,x,z,accent,phase){const g=new THREE.Group();g.position.set(x,0,z);const body=new THREE.Mesh(new THREE.CapsuleGeometry(.23,.42,5,12),M(0xe7ebef,.25,.28));body.position.y=.72;body.castShadow=true;g.add(body);const head=new THREE.Mesh(new THREE.SphereGeometry(.31,22,16),M(0xf5f7f8,.18,.22));head.scale.y=.78;head.position.y=1.27;head.castShadow=true;g.add(head);box(g,.43,.14,.045,0x10151b,0,1.27,-.28,.55,.2);[-.105,.105].forEach(px=>{const e=new THREE.Mesh(new THREE.SphereGeometry(.032,10,8),new THREE.MeshBasicMaterial({color:accent}));e.position.set(px,1.27,-.307);g.add(e)});const chest=new THREE.PointLight(accent,1.2,1.8,2);chest.position.set(0,.78,-.25);g.add(chest);parent.add(g);bots.push({g,phase,baseX:x,baseZ:z});return g}
 function desk(parent,x,z,accent,rot=0){const g=new THREE.Group();g.position.set(x,0,z);g.rotation.y=rot;box(g,1.45,.12,.62,0xa89473,0,.68,0,.02,.52);[-.55,.55].forEach(px=>box(g,.07,.65,.07,0x32383e,px,.34,0,.35,.45));box(g,.62,.42,.045,0x121820,0,1.02,-.13,.5,.18);glow(g,.54,.32,accent,0,1.02,-.155,0,.75);parent.add(g)}
 function room(i){const a=i*Math.PI/4, r=9.05, g=new THREE.Group();g.position.set(Math.sin(a)*r,0,Math.cos(a)*r);g.rotation.y=a;scene.add(g);const accent=themes[i];box(g,5.0,.16,4.0,0xd8d5ce,0,.02,0,.02,.9);box(g,5.0,2.35,.13,0xdedbd4,0,1.18,1.94,.02,.82);box(g,.13,2.35,4.0,0xcecbc5,-2.44,1.18,0,.02,.85);box(g,.13,1.3,2.2,0xcecbc5,2.44,.65,.7,.02,.85);const glass=new THREE.Mesh(new THREE.BoxGeometry(2.1,1.25,.045),new THREE.MeshPhysicalMaterial({color:0x7ba5c7,transparent:true,opacity:.24,roughness:.06,metalness:.05,transmission:.3}));glass.position.set(.85,1.35,1.84);g.add(glass);box(g,2.1,.045,.06,accent,.85,2.0,1.82,.25,.3);const light=new THREE.PointLight(accent,4.5,6.5,2);light.position.set(0,2.1,.3);g.add(light);desk(g,.72,.1,accent,Math.PI);desk(g,-1.05,-.92,accent,Math.PI);robot(g,.72,-.55,accent,i*.7);robot(g,-1.05,-1.55,accent,i*.7+.35);for(let p=0;p<2;p++){const pot=new THREE.Mesh(new THREE.CylinderGeometry(.17,.23,.3,14),M(0x5b4531,.02,.85));pot.position.set(p?1.95:-1.9,.15,1.15-p*.2);g.add(pot);const leaf=new THREE.Mesh(new THREE.SphereGeometry(.32,14,10),M(0x315f43,.01,.85));leaf.scale.set(.7,1.15,.7);leaf.position.set(p?1.95:-1.9,.55,1.15-p*.2);g.add(leaf)}
 // Department sign.
 const cv=document.createElement('canvas');cv.width=420;cv.height=110;const cx=cv.getContext('2d');cx.fillStyle='rgba(9,13,19,.94)';cx.fillRect(0,0,420,110);cx.strokeStyle='#cfa65c';cx.lineWidth=4;cx.strokeRect(3,3,414,104);cx.fillStyle='#f6eddb';cx.font='700 34px Arial';cx.textAlign='center';cx.textBaseline='middle';cx.fillText(names[i],210,55);const tx=new THREE.CanvasTexture(cv);tx.colorSpace=THREE.SRGBColorSpace;const sp=new THREE.Sprite(new THREE.SpriteMaterial({map:tx,transparent:true}));sp.position.set(0,2.7,1.65);sp.scale.set(2.7,.7,1);g.add(sp)}
 for(let i=0;i<8;i++)room(i);
 // Command core.
 const hub=new THREE.Mesh(new THREE.CylinderGeometry(2.35,2.55,.38,64),M(0x131b26,.58,.32));hub.position.y=.2;hub.receiveShadow=true;scene.add(hub);
 [1.8,1.25].forEach((r,i)=>{const t=new THREE.Mesh(new THREE.TorusGeometry(r,.045,10,72),new THREE.MeshBasicMaterial({color:i?0xd8ae5d:0x6488ff,transparent:true,opacity:.95}));t.rotation.x=Math.PI/2;t.position.y=.46+i*.08;scene.add(t)});
 const core=new THREE.Mesh(new THREE.IcosahedronGeometry(.78,3),new THREE.MeshPhysicalMaterial({color:0x7690ff,emissive:0x263fbb,emissiveIntensity:3.1,metalness:.12,roughness:.05,transparent:true,opacity:.88}));core.position.y=1.42;scene.add(core);const wire=new THREE.Mesh(new THREE.IcosahedronGeometry(1.15,2),new THREE.MeshBasicMaterial({color:0x8ca4ff,wireframe:true,transparent:true,opacity:.22}));wire.position.y=1.42;scene.add(wire);const coreLight=new THREE.PointLight(0x6688ff,8,9,2);coreLight.position.set(0,1.5,0);scene.add(coreLight);
 // Architectural lighting.
 scene.add(new THREE.HemisphereLight(0x8da8d8,0x17110b,1.15));const key=new THREE.DirectionalLight(0xffe0ad,3.0);key.position.set(-8,16,-5);key.castShadow=true;key.shadow.mapSize.set(2048,2048);key.shadow.camera.left=-20;key.shadow.camera.right=20;key.shadow.camera.top=20;key.shadow.camera.bottom=-20;scene.add(key);const rim=new THREE.DirectionalLight(0x5c83ff,2.1);rim.position.set(12,9,12);scene.add(rim);
 // HUD overlay.
 const hud=document.createElement('div');hud.style.cssText='position:absolute;inset:14px 16px auto 16px;display:flex;justify-content:space-between;pointer-events:none;z-index:3;font-family:inherit';hud.innerHTML='<span style="padding:7px 10px;border:1px solid #d4a94f55;border-radius:10px;background:#07101bcc;color:#e8c06c;font-size:10px">NAWAF HQ • 3D LIVE</span><span style="padding:7px 10px;border:1px solid #5f84ff55;border-radius:10px;background:#07101bcc;color:#91a9ff;font-size:10px">16 AI WORKERS</span>';host.appendChild(hud);
 let theta=.64,radius=31,camY=22,drag=false,last=0;function aim(){camera.position.set(Math.sin(theta)*radius,camY,Math.cos(theta)*radius);camera.lookAt(0,.4,0)}aim();renderer.domElement.addEventListener('pointerdown',e=>{drag=true;last=e.clientX});window.addEventListener('pointerup',()=>drag=false);renderer.domElement.addEventListener('pointermove',e=>{if(!drag)return;theta+=(e.clientX-last)*.004;last=e.clientX;aim()});renderer.domElement.addEventListener('wheel',e=>{e.preventDefault();radius=Math.max(23,Math.min(38,radius+e.deltaY*.012));camY=Math.max(16,Math.min(27,camY+e.deltaY*.006));aim()},{passive:false});
 const clock=new THREE.Clock();function loop(){requestAnimationFrame(loop);const t=clock.getElapsedTime();core.rotation.y=t*.35;core.rotation.x=t*.12;wire.rotation.y=-t*.18;wire.rotation.z=t*.08;bots.forEach((b,i)=>{b.g.position.y=Math.sin(t*1.8+b.phase)*.035;b.g.rotation.y=Math.sin(t*.55+b.phase)*.06});renderer.render(scene,camera)}loop();new ResizeObserver(()=>{if(!host.clientWidth||!host.clientHeight)return;camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix();renderer.setSize(host.clientWidth,host.clientHeight)}).observe(host);
}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,300)):setTimeout(boot,300);
})();